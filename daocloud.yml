version: 2.0

test:
  image: daocloud/ci-python:3.4

  install:
    - pip install -Ue .

  before_script:
    - whoami
    - locale-gen zh_CN.UTF-8 && update-locale LANG=zh_CN.UTF-8
    - locale
    - echo $LANG
    - export LANG=zh_CN.UTF-8
    - locale
    - echo $LANG

  script:
    - python b4m/libs/encryptor.py

build:
  image:
    dockerfile_path: Dockerfile

    build_dir: /

    cache: true

