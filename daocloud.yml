version: 2.0

test:
  image: daocloud/ci-python:3.4

  env:
    - LANG = "C.UTF-8"

  install:
    - pip install -Ue .

  script:
    - python b4m/libs/encryptor.py

build:
  image:
    dockerfile_path: Dockerfile

    build_dir: /

    cache: true

